{
  "scripts": {
    "start": "rm -rf dist/; parcel src/index.html",
    "build": "rm -rf dist/; parcel build src/index.html",
    "github": "parcel build src/index.html --dist-dir ../navitux.github.io/"
  },
  "dependencies": {
    "parcel": "^2.8.3"
  }
}
